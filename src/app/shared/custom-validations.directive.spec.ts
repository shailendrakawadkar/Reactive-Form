import { CustomValidationsDirective } from './custom-validations.directive';

describe('CustomValidationsDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomValidationsDirective();
    expect(directive).toBeTruthy();
  });
});
